dialog:
  blueprint_settings:
    title: Blueprint Settings
    pages:
      general:
        label: General
      data_pack:
        label: Data Pack
      resource_pack:
        label: Resource Pack

config:
  blueprint_settings:
    export_environment:
      label: Export Environment
      description: Choose the environment you will export the Blueprint for.
      options:
        vanilla: Data Pack & Resource Pack
        plugin: Plugin

    show_render_box:
      label: Show Render Box
      description: Toggle the visibility of the render box

    auto_render_box:
      label: Auto Render Box
      description: Automatically adjust the render box to fit the model
